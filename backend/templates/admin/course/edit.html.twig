{% extends 'base.html.twig' %}

{% block title %}Modifier le cours : {{ course.title }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-warning text-dark">
                    <h1 class="h3 mb-0">✏️ Modifier le cours</h1>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        
                        {{ form_errors(form) }}

                        <div class="mb-4">
                            {{ form_row(form.title) }}
                        </div>

                        <div class="mb-4">
                            {{ form_label(form.pdfFile) }}
                            
                            {% if course.pdfFilename %}
                                <div class="alert alert-info py-2 mb-2 d-flex align-items-center">
                                    <span class="me-2">📄 Fichier actuel : <strong>{{ course.pdfFilename }}</strong></span>
                                </div>
                            {% endif %}
                            
                            {{ form_widget(form.pdfFile) }}
                            <div class="form-text text-muted">
                                Laissez vide si vous ne voulez pas changer le fichier PDF.
                            </div>
                            {{ form_errors(form.pdfFile) }}
                        </div>

                        <div class="mb-4">
                            {{ form_row(form.description) }}
                        </div>

                        <div class="mb-4 p-3 bg-light rounded border">
                            {{ form_row(form.generate_description) }}
                        </div>

                        <div class="d-flex justify-content-between align-items-center mt-4">
                            <a href="{{ path('admin_course_index') }}" class="btn btn-outline-secondary">
                                ← Retour à la liste
                            </a>
                            
                        </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}